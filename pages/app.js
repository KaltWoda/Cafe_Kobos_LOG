// Este archivo es necesario para que Next.js cargue las páginas correctamente
// Puedes agregar estilos globales aquí si quieres

export default function App({ Component, pageProps }) {
  return <Component {...pageProps} />
}
